;;; dpb-appearance.el --- Visual appearence

;;; Commentary:
;; 

;;; Code:

(setq frame-title-format '( "%F -=     %b     =- %m" ))
;(require 'frame-restore)
;; (eval-and-compile
;;         (autoload 'frame-restore          "frame-restore"))

;; (frame-restore)

(provide 'dpb-appearance)

;;; dpb-appearance.el ends here
